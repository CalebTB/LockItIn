# FLOW 1: ONBOARDING (First-Time User)

```markdown
Launch App
↓
Welcome Screen → "Your calendar, supercharged for friend groups"
↓
Sign Up / Log In
↓
Request Calendar Access → "We'll sync with Apple Calendar"
↓
Request Notifications → "Get notified about group events"
↓
Calendar View (empty or synced from Apple Calendar)
↓
Tooltip: "Tap + to create an event" / "Swipe up for month view"
```